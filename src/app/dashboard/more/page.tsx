import { MoreClientView } from "../../../features/more/components/MoreClientView";

/**
 * More / Settings Page.
 * Provides access to user profile, application settings, and logout functionality.
 */
export default async function MorePage() {
	return <MoreClientView />;
}
